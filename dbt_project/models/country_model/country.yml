
version: 2

models:
  - name: countries
    description: "A custom country model"
    columns:
      - name: Country_name
        description: '{{doc("table_events")}}'
        tests:
          - unique
          - not_null
          - country_name
          

  